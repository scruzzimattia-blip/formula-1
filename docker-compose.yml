version: '3.8'

services:
  f1-dashboard:
    build: .
    container_name: f1-dashboard
    ports:
      - "8501:8501"
    volumes:
      - ./fastf1_cache:/app/fastf1_cache
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
